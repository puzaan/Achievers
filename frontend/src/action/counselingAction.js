import axios from 'axios'
import {} from 'constants/counseling'

export const CreateCounselling = (name, email, number, course, date, counselling,) => async(dispatch) => {
    try {
        
    } catch (error) {
        
    }
}